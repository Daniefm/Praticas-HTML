//Declarando variaveis.
var i, num = parseInt(prompt("Informe um número: "));

soma = 0;

//Fazendo sequencia de números.
     document.write("<div>Sequencia de números: ");
       for(i = 0; i <= num; i++){
     document.write(i + ", ");


// Somando os números ímpares.
       if(i % 2 == 0);
       
       else{
           soma = soma + i;
       }
     
    
    } document.write("<br><br>Soma dos números ímpares: " + soma + "</div>");